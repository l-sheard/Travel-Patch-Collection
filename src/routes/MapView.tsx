import { MapIcon } from '../components/layout/icons'
import PlaceholderPage from '../components/PlaceholderPage'

export default function MapView() {
  return (
    <PlaceholderPage
      icon={MapIcon}
      title="Your travels"
      description="Every patch plotted on a world map by where you got it. Click a pin to jump to that patch's details."
    />
  )
}
