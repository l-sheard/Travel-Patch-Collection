import { PlusIcon } from '../components/layout/icons'
import PlaceholderPage from '../components/PlaceholderPage'

export default function AddPatch() {
  return (
    <PlaceholderPage
      icon={PlusIcon}
      title="Add a patch"
      description="Log a new patch with its location, trip dates, purchase date, travel companions, description, and photos."
    />
  )
}
