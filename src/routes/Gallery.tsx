import { GalleryIcon } from '../components/layout/icons'
import PlaceholderPage from '../components/PlaceholderPage'

export default function Gallery() {
  return (
    <PlaceholderPage
      icon={GalleryIcon}
      title="Patch gallery"
      description="A grid of your patches with backgrounds removed, so each one reads like a clean sticker on the shelf."
    />
  )
}
